// const { withNextVideo } = require('next-video/process')

// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactStrictMode: true,
// }

// module.exports = withNextVideo(nextConfig)


// next.config.js
module.exports = {
  images: {
    domains: ['tenor.com'], // Add the domain of your image here
  },
};
